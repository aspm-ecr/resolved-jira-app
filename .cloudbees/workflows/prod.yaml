apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: jfa-cb-workflow
          artifact-id: 1768f962-c060-41e7-a200-d366992b0904
          target-environment: aspm-qa-test