apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: jfa-cb-workflow
          artifact-id: f6eff42e-054e-47d1-837e-1ab213b3b987
          target-environment: aspm-qa-test