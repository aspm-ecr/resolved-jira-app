apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: jfa-cb-workflow
          artifact-id: a4bacbc4-f034-499b-a652-22f806606dc6
          target-environment: aspm-qa-test